import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'envia-mensagens-whatsapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
